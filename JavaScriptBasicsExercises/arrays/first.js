// returns the first and last element of an array
function first(array) {
  return array[array.length - 1];
}

energy.shift()
energy.push('geothermal');

alphabet.split('');

//returns the number of scores equal to or over 100 in the array
scores.filter(num => num >= 100).length;
